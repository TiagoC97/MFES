class TrainingSessionTest is subclass of MyTestCase

instance variables

	trainer1: Trainer := new Trainer("Vasco", 25, <Male>, "brazilian");

	client1: Client := new Client("Maria", 23, <Female>, "portuguese");

  	trainingSession1: TrainingSession := new TrainingSession("Aula iniciante", client1, <Monday>, 
		Utils`CreateHour(14, 00), 
	  	Utils`CreateHour(15, 00), 
	  	Utils`CreateDate(2019, 01, 14));
	
	
operations
	public Run: () ==> ()
	Run() == (
		IO`println("\nTrainingSession Tests");

		assertEqual(client1.getID(), trainingSession1.getTrainee());

		IO`println("Finalizing TrainingSession Tests");

	);
	
end TrainingSessionTest